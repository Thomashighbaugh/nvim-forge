-- At the end of the day, I like my own brand
-- FEATURING: My base16 colorscheme "Vice"!
return {
    "RRethy/nvim-base16",
    config = function()
        vim.cmd("colorscheme base16-vice")
    end,
}
